# 仅供后端使用，绝对不要放进 Expo 客户端或提交真实密钥。
OPENAI_API_KEY=
# 选择一个支持图片输入和 Structured Outputs 的模型。
OPENAI_MODEL=gpt-5.6
# 可选：服务端默认的 OpenAI-compatible Base URL，例如 https://api.openai.com/v1
OPENAI_BASE_URL=
PORT=8787
# 生产环境建议设置为你的 App 后端域名；原生 App 可保持 *。
ALLOWED_ORIGIN=*
